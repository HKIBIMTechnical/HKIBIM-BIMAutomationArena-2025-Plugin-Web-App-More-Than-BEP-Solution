import streamlit as st

st.title("Civil 3D Layer Cleaning")

st.subheader("""
In Civil 3D when doing design work and there are a lot of layers exist in file if we open dwg file, but acturally if we open layer management to review layer then name or color or other properties we can get and we can not get amount of all elements inside layer, so GT provide this solution to fast review amount of all element inside layer then to determine if layer can be deleted, see below screenshot to view GT plugin for Civil 3D → “LayerElementInfoTable_Form”, it can help you to clean layers.
""")
st.image(r"pics/Workflow/Civil_3D_Layer_Cleaning/pic1.png",use_container_width=True)




